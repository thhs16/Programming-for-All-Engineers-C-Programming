#include <stdio.h>

int main(){

        int gross, perR;

        float taxR, bonus, dd, result;

        scanf("%d", &gross);
        // scanf("%.2f", &taxR);
        // scanf("%d", &perR);
        printf("hello");


        // switch(perR){
        //     case 5: bonus = gross * 20 / 100; break;
        //     case 4: bonus = gross * 10 / 100; break;
        //     case 3: bonus = gross * 5 / 100; break;
        // }

        // dd = gross * taxR / 100;

        printf("%d", gross);

        return 0;
}