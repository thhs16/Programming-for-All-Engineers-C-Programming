// Name : Thae Htape Htar San
// ID : 68070503468
// PURPOSE :

#include <stdio.h>

    // int even(){
    
    // }

int main(){

        long num[5];

        for (int i = 0; i < 6; i++)
        {
            scanf("%d", &num[i]);
        }
        
        for (int i = 0; i < 6; i++)
        {
            printf("%d", num[i]);
        }
        // printf("Hello word");
        
        return 0;
}