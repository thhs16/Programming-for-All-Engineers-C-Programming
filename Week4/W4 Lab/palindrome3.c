#include <stdio.h>

int main(){

        double num;

        num = 0;

        // positive or negative
        if ( num <= 0 ){
            printf("Negative value is not considered a palindrome.");
        }

        // finding the length odd or even
        
        
        return 0;
}